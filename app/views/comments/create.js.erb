$('.comments').append("<%= escape_javascript(render 'comments/comment', comment: @comment) %>");
$("body").overhang({
  type: "success",
  message: "تم إضافة التعليق بنجاح!"
});
$('#comment_content').val('')