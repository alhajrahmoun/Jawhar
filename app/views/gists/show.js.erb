$('.gists').html("<%= escape_javascript(render 'gists/gists', gists: @gists, gist: @gist, active: @gist.id) %>");
$('.snippets').each(function() {
    var editor = CodeMirror.fromTextArea(this, {
        lineNumbers: true,
        matchBrackets: true,
        readOnly: true
    });
});
$('.last-gists').scrollTo('#active');